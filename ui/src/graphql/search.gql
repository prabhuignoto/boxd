query search($query: String!) {
  search(query: $query) {
    matches {
      metadata {
        name,
        tag,
        id,
        server_modified,
        size,
        path_lower,
        content_hash
      }
    }
  }
}